/**
 *	jQuery Document Ready
 */
jQuery( document ).ready( function($) {
	$( window ).load( function() {
		$( '.wp-full-overlay-sidebar-content a' ).first().attr( 'href', 'http://themeisle.com/themes/rokophoto?ref=6148' );
	});
});